<h1>User List</h1>

<table>
  <tr>
    <th>Eail</th>
    <th>First name</th>
	<th>Last name</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
        <td><%= user.email %></td>
		<td><%= user.first_name %></td>
		<td><%= user.last_name %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Back', projects_path %>